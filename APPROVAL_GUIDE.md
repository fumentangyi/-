# 工部审批机制与日志系统说明

## 概述

本系统为三省六部制AI Agent系统添加了工部技术方案审批机制和完整的日志系统。

## 新增功能

### 1. 工部审批机制

工部节点现在具备以下能力：
- **收集各部门反馈**：从吏、户、礼、兵、刑五部收集技术方案优化建议
- **生成优化方案**：基于各部门反馈生成技术优化方案
- **提交审批申请**：将优化方案提交给尚书省审批
- **用户最终确认**：方案通过门下省审核后，需要用户最终确认

### 2. 审批流程

```
工部 → 尚书省审批 → 门下省审核 → 用户确认 → 执行优化方案
  ↓        ↓            ↓          ↓           ↓
反馈     可行性评估    合规性审核    Y/N决策    实施执行
收集
```

### 3. 日志系统

所有系统操作都会记录到日志文件中，日志文件保存在 `logs/` 目录，按日期命名：
- 格式：`execution_YYYYMMDD.log`
- 内容：包含时间戳、日志级别、执行阶段、具体信息

## 使用方法

### 命令行模式

```bash
# 运行主程序
python core_architecture.py

# 输入任务后，系统会自动执行
# 当工部提交审批申请时，会在终端显示审批信息
# 用户需要在5分钟内输入 Y（批准）或 N（拒绝）
```

### Web界面模式

```bash
# 启动Web界面
streamlit run web_app.py

# 在浏览器中打开 http://localhost:8501
# 输入任务并点击"开始执行"
# 审批信息会显示在"工部审批"选项卡中
```

### 测试模式

```bash
# 运行测试脚本
python test_approval.py
```

## 工作流程详解

### 1. 常规任务流程

```
联网搜索 → 中书省制定方案 → 门下省审核 → 尚书省执行 → 门下省终审 → 反思
```

### 2. 工部审批流程

当任务需要技术方案优化时，会触发工部审批流程：

1. **收集反馈**：工部向各部门收集技术优化建议
2. **生成方案**：工部基于反馈生成优化方案
3. **尚书省审批**：评估技术可行性
4. **门下省审核**：评估合规性
5. **用户确认**：最终批准或拒绝方案
6. **执行优化**：实施优化方案

## 日志级别

- **INFO**：正常操作信息
- **WARN**：警告信息（如驳回、重新提交）
- **ERROR**：错误信息

## 配置参数

- 用户确认超时时间：300秒（5分钟）
- 驳回重试限制：3次

## 注意事项

1. 确保Ollama服务正在运行
2. 确保已安装所有依赖包（streamlit, langchain, langgraph等）
3. 日志文件会自动创建在logs目录
4. 用户确认超时会自动拒绝审批

## 扩展开发

如需自定义审批流程，可以修改以下文件：
- `core_architecture.py`：核心逻辑和工作流
- `web_app.py`：Web界面
- `logger.py`：日志系统

## 问题排查

如果遇到问题：
1. 检查logs目录中的日志文件
2. 确认Ollama服务状态：`ollama list`
3. 检查网络连接（用于联网搜索）
4. 查看Web界面的错误信息
